kumuluzee:
  # name of our service
  name: test-service
  version: 1.0.0
  server:
    # url where our service will live
    base-url: http://localhost:5000
    http:
      port: 5000
      address: http://localhost
  env: 
    name: dev
  config:
    namespace: ''
    start-retry-delay-ms: 0
    max-retry-delay-ms: 0
    etcd:
      hosts: http://localhost:2379
    consul:
      hosts: http://localhost:8500
  discovery:
    ttl: 100
    ping-interval: 20
    consul:
      hosts: http://localhost:8500
    etcd:
      hosts: http://localhost:2379
settings:
  location: Oštrc
  house:
    owner:
      name: Alen
      age: 22
    sale: Y
    parts: "streha,vrata,okna,stene,pohištvo"
